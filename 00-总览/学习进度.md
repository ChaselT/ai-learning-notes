---
tags: [AI学习, 总览, 进度]
created: 2026-07-24
---

# 学习进度

> 起点：2026-07-24（第 1 周从 2026-07-27 周一起算）。每完成一项就勾选，每 4 周在下方写一段复盘。

## 里程碑

> 每个阶段末尾的「阶段测试 ≥80 分」是进入下一阶段的硬门槛。

- [ ] 第 2-3 周末（2026-08-09，最迟 2026-08-16）：环境就绪，Python 异步脚本跑通 + **[[阶段0测试]] 过关**（零基础起步，允许用到 3 周）
- [ ] 第 8 周末（2026-09-20）：项目① CLI 智能助手上 GitHub + **[[阶段1测试]] 过关**
- [ ] 第 16 周末（2026-11-15）：项目② 知识库问答完成并有评估报告
- [ ] 第 24 周末（2027-01-10）：项目③ Agent 应用 Docker 部署 + 全链路追踪
- [ ] 第 31 周末（2027-02-28）：项目⑤ YOLO 图像识别接口上线演示
- [ ] 第 36 周末（2027-04-04）：项目④ 微调模型接入 Agent + 复盘博客
- [ ] 第 37 周起（2027-04-05）：开始投递，边面边学

## 阶段 0 清单（第 1-3 周，零基础起步）

- [x] 安装 uv、初始化 phase0-python 项目（2026-07-24 已完成）
- [x] 第一轮：基础篇 9 篇读完 + 动手任务（2026-07-27 收官，历时 4 天，27 个练习全过）
- [x] 第二轮：进阶篇 7 篇读完 + 动手任务（2026-07-29 收官）
- [ ] 独立写一个带类型注解的异步小脚本并跑通
- [ ] 完成 [[阶段0测试]]（笔试+实操），批改 ≥80 分

## 每周记录

### 第 0 周（2026-07-24，正式开始前的首次会话）
- 文档重构：学员自纠 Python 零基础，阶段 0 改为基础篇（9 篇）+ 进阶篇（8 篇）两轮
- **首日学习成果（超预期）**：
  - [x] uv 篇全部任务过关：验证项目、notes_pyproject.md 对照 pom.xml、check_env.py
  - [x] 真实事故修复：uv add 覆盖丢失 httpx[socks]，自行定位并修复（学会 extras 概念）
  - [x] `if __name__ == "__main__"` 原理 + demo_import 实验验证
  - [x] 基础01 过关：b01 三个练习 + 检验题全对（/ 与 // 差异、ZeroDivisionError 预判）
- 掌握的调试习惯：先看文件内容再排查、traceback 从下往上读
- [x] 基础02 过关（晚场 21:35-22:00，白天摸鱼预习奏效）：b02 三练习完成，切片/步长检验题全对；自修 f-string `.2f` 格式 bug
### 第 2 天（2026-07-25 周六）
- [x] 基础03 过关：b03 三练习；检验题订正一处（*rest 恒为 list）；掌握 sort() 返 None 大坑
- [x] 基础04 过关：b04 三练习全对（dict 计数惯用法、集合运算）；自发超纲用 for；纠出文档措辞错误（班级→课程，逻辑推理出交集恒空）
- [x] 基础05 过关：b05 三练习，修复三处——range 含头不含尾漏了 30（off-by-one）、三元表达式改为产出值、scores/score 命名遮蔽；truthy 全猜对
- 基础篇进度 **5/9**，节奏：白天预习+晚上动手，单篇 30-40 分钟
- 调研：无增量变化
### 第 3 天（2026-07-26 周日）
- [x] 基础06 过关：b06 三练习（默认/关键字参数、*args+解包、apply_all 高阶函数）；自发用列表推导式并通过"循环+收集"本质验证
- 修复清单：print→return、"25"→25 类型糊弄、函数定义收回模块顶层、stats() 空调用遗留雷、护栏只防 avg 不防 min/max
- Ruff format on save 确认生效
- [x] 基础07 过关（周日晚加场，22:30-23:15）：Book/__repr__、继承多态、BankAccount property 三练习；经历 property 双层 bug（类赋值覆盖描述符 → 同名无限递归）并修复；自行实验验证 super() 不自动调用（创建静默、访问才炸）；理解方法脱糖 `obj.m(x) ≡ Class.m(obj, x)`
- 基础篇进度 **7/9**
### 第 4 天（2026-07-27 周一）
- [x] 基础08 过关（午休摸鱼场！12:39-13:31 远程完成）：safe_input/custom_exc/chain 三练习；修复三处——死分支 except TypeError、try 块内第二次调用永不执行、异常链缺 from e；掌握回溯两截读法与 ValueError/TypeError 分工（"值的锅重输，类型的锅修代码"）
- [x] 基础09 过关（晚场）：模块自测/跨文件导入/标准库四件套；含 PI 命名、容器复数（第二犯）、date vs datetime 三个微修
- [x] 收官彩蛋：用推导式重构 b02_csv 的三行 strip——第一次"新知识回头优化旧代码"
- 🎉 **基础篇 9/9 收官**（7-24 ~ 7-27，4 天，计划 1-1.5 周）
- 调研：无增量变化（MCP 明日发布）
- [x] 进阶篇 1/7《核心差异》总复习过关（当晚顺势完成）：首日三件套销账——diff_basics（含补全列表 is 实验）、duck_typing（自产 rander 拼写 bug，亲历鸭子类型无编译期保护）、asyncio 思考题（预习版答案，async 篇正式回收）；主动用 Java Integer 缓存类比推理出小整数缓存池
### 第 5 天（2026-07-28 周二）
- [x] 进阶篇 2/7《类型注解与typing》过关：parse_scores 注解全绿；Protocol 三件套 + 外科手术式破坏实验（亲见 [arg-type] 结构冲突报错——"能抓住 rander 的那双眼睛"）
- 修复清单：拼写三连击（boadcast→boardcast→broadcast，正式立户头）、签名按规格（list+循环+None）、Protocol 体改 `...`、删多余 None 防御、main 加 -> None（免检区教训）
- 新武器：mypy + Pylance typeCheckingMode=basic（默认 off，已开启）
- 调研：MCP 新规范当日如期发布，无意外；阶段 3 Session 设计要点已记入 PLAN
- [x] 进阶篇 3/7《dataclass与pydantic》过关（同日午休场！）：dataclass 按值比较、pydantic Field 约束/ValidationError 聚合（双错实验：1 个异常聚合 2 条错误——将来 LLM 输出修复回喂的设计依据）、model_json_schema 嵌套 Schema（$defs/$ref + required 合同条款意识）
- 修复清单：dataclasses.field 混进 BaseModel（应用 pydantic 的 Field）、实例 dump vs 类 schema 一字之差、except 变量 e 复用（mypy 抓获）、残渣第三犯
- [x] 进阶篇 4/7《装饰器》主体完成（晚场）：log_call 三版（基础/带参/超纲双模版，含 @log_call("DEBUG") 破坏实验记录）、retry(times, delay)；修复历程——透传裸调 fn()、match 三重复分支（WARN 静默吞函数隐患）、sleep 位置（放弃前白睡）、kwargs 通路验证
- [x] 《装饰器与上下文管理器》任务 3 收尾：cd(path) 三场景测试（正常/进门失败/屋内着火），结构修正为"获取在 try 外、try 只包 yield"——**进阶篇 4/7 整课过关**
- 当日杂务：仓库拆分三体系、作业骨架机制、全历史 filter-repo 脱敏、README×2、装饰器笔记补三级台阶
### 第 6 天（2026-07-29 周三）
- [x] 进阶篇 5/7《async异步编程》过关：逐个 await vs gather（5s vs 1s）；三组对照实验（纯异步 0.95s / 异步睡眠 3.20s / 同步阻塞 4.36s）用时间戳证明"让路 vs 堵路"；asyncio vs 多线程的答案完成修订
- 经典坑双入册：E27 协程未 await（RuntimeWarning=实验作废）、E28 协程内同步阻塞冻结事件循环
- 错题本已升级为卡片式（26→28 张，❌/✅/机制/折叠自测/打卡）
- [x] 进阶篇 6/7《常用标准库与生态》过关（下午场）：三种推导式 + enumerate(start=1)（修 zip 单元素元组 bug）、logging/耗时/pathlib 闭环（含 __file__ 锚点、ensure_ascii 升级）、生成器函数 + 生成器表达式 sum 计数；质疑"(c) 是否该用推导式"获认证——原则内化
- 环境事件：httpbin.org 503，全部端点切换 httpbingo.org
- [x] 进阶篇 7/7《Jupyter与调试》过关（晚场）：experiment.ipynb 全流程（含自行发现模块缓存坑→autoreload 方案）、断点调试定位负数最大值 bug、notebook→模块沉淀（IO 与处理分离、免检区/Any 逃逸两次骗过 mypy 的实战课）
- 🏁 **阶段 0 全部 17 课收官**（2026-07-24 ~ 07-29，6 天，计划 2-3 周）；错题本 28 卡
## 阶段 1：LLM 应用开发（2026-07-30 起）

- [x] **1/11 大语言模型工作原理速览**（07-31）：两类幻觉实验——自行发现"ZGC 是 GC 不是 JVM"；探针二分法测出 qwen3.5:27b 训练数据停在 2025 初（模型自报三答三不同）；显存估算 Q4_K_M 混合精度
- [x] **2/11 Token与上下文窗口**（07-31）：中英文双维度实测**推翻教材旧结论**（字符效率英文高 4.3 倍，同等语义基本持平）；emoji/生僻字按频率定价；多轮 token 膨胀与 trim_history 对照实验，实证"按条数截断约束不了成本"
- [x] **3/11 Chat Completions API**（07-31）：三练习（首调用+异常分层、多轮对话失忆实验、后端切换）；发现 thinking 模型输出 token 差 52 倍；发现中转站会丢失 reasoning_tokens 等扩展字段
- 教材修正 4 处：骨架 drift、Q3 老结论、课程顺序（ex02 前置依赖）、缺上下篇导航
- 错题本 E32-E35 入册；**已开启 mypy `disallow_untyped_defs`**（免检区第 4 次复犯后的工具兜底）
- [x] **4/11 采样参数详解**（08-01）：温度梯度实验（T=0 九成一致 / T=1.4 小模型直接崩坏）；**做出关键对照——同代码只换 base_url，本地生效、中转站静默吞参数**；用 set 量化确定性 90% 并解释首次不同的成因；自发打印 system_fingerprint
- 教材第 5 处修正：采样参数笔记原本只有"锐化/钝化"比喻、无机制说明 → 补 logits÷T 数学原理 + 参数生效判定三法（学员主动指出"知识库没有"）
- 错题本 E36 入册
- [x] **5/11 流式输出与SSE**（08-03）：三练习——TTFT 对照（**三轮实验才把变量控住**，两次自我推翻）、流式多轮对话（含流中断降级处理，超纲）、异步并发流式（交错输出实证 + 发现 `OLLAMA_NUM_PARALLEL=1` 导致的服务端排队）
- 关键认知：`print(flush=True)` 吃掉 45% 耗时（测量工具影响被测对象）；客户端并发 ≠ 服务端并发；本地单卡并发收益有限（算力共享，并发时单任务耗时翻倍）
- 错题本 E38 入册；**学员开始质疑教练给的方案**（"为什么两个流式都要加 usage"——用控制变量思维反推指令，判断正确）
- [x] **6/11 Prompt工程**（08-03）：三任务全过——①**自创三级对照实验**（完整/删拒答规则/无 prompt），发现"被角色绑架、一本正经胡说"的中间态；②zero-shot vs few-shot 4/7 vs 7/7，发现 few-shot 能教会模型未明说的规则（年薪换算）但覆盖面受限于示例；③prompt 外置到 `prompts/*.md` + `Path(__file__).parent` 锚点
- 错题本 E39/E40 入册
- ⚠️ **教练侧问题记录**：Claude 三次因"只读文件片段"（tail/sed 抽样）就下断言而误判学员成果——已改为 review 前完整读文件
- [x] **7/11 结构化输出**（08-04）：三任务全过——①json mode 成功率对照 10/10 vs 5/10，**自行加做 0.5b 小模型对照组**（1/10）定位 token 异常真因；②pydantic 校验 + 回喂自我修复，**自行补无 schema 对照组**才触发出真实 ValidationError，拿到"失败→失败→成功"三级日志；③口语化 Todo 解析，撞出"模型把 schema 当答案复读"
- 关键认知：**校验通过 ≠ 数据正确**（"八十年"被静默截断成 50、无技能被编造）；给模型看 schema = 把校验从事后检测变成事前诱导，代价是看不见脏数据；`openai.omit` 哨兵表达"参数不传"（≠ 传 None）；先可观测再谈重试策略
- 错题本 E41/E42/E43 入册
- ⚠️ **教练侧问题记录（第二类）**：Claude 连续两次拿 usage 分栏数据下结论且**前提查证不足**（误判在用中转站、误称"省 10 倍 token"），均由学员实测推翻——**跨配置比 token 成本只看 total**
- [x] **8/11 Function Calling**（08-05）：两任务全过——①完整工具闭环（tool_calls → 执行 → role=tool 回填），破坏性实验**做了两轮**才复现（只抹 description 时三个模型全无感，把 name/参数名/参数 description 一并中性化后才崩）；②4 工具注册表 + read_file 沙箱，**自行修复目录穿越漏洞并消除 oracle**
- 关键认知：description 的职能是**区分同构工具**，缺失时模型不停手而是瞎调；**工具的错误信息会引导模型下一次调用**（实测：拿到"暂无数据"后自行把中文参数换成英文重试），所以报错要写给模型看；工具返回值会进上下文，必须截断且显式告知；路径沙箱定序 resolve → 判边界 → 判存在
- 新语法点：`openai.omit` 哨兵、判别式联合类型收窄（`tc.type == "function"`）、`Callable[..., str]` 表达动态分发、`typing.assert_never` 穷尽性检查
- 错题本 E44/E45 入册
- ⚠️ **教练侧问题记录**：Claude 给"先判边界再判存在"的建议时漏说"规范化必须最先"，导致学员改出穿越漏洞；后由实测推演发现。**建议涉及顺序时必须给全序列**
- [x] **9/11 多模态视觉API**（08-06）：三任务全过——①报错截图分析；②发票 OCR → 嵌套 pydantic（Decimal/date/Party/InvoiceItem），业务校验发现「明细合计 ≠ 价税合计」；③云端 vs 本地 + **分辨率-成本-精度曲线**（自行把骨架的两档扩成四档）
- 关键认知：成本曲线是 **U 型**，最优点 600px（总 504 tokens、精度 100%）；压到 300px 总成本 1582 反而是 600px 的 3.1 倍且精度掉到 88.89%——**输入 token 单调下降会骗人，决策看 total**；**输出 token 暴增（86→1475）是"模型对该输入没把握"的可观测信号**，可作质量兜底的触发条件；识别失效集中在图表最左侧贴 Y 轴处（4 个错格有 2 个在第 1 月）
- 工程点：素材做成可复现脚本（`chart_fixture.py`，图由数据渲染 → ground truth 与图天然一致）；评测函数返回 `NamedTuple`，把「键缺失/多余/长度不符」与「读数错误」分开报，避免结构问题伪装成准确率下降（**这是 eval 体系与"跑个分"的分界线，直接对口 video-clipper-pro 的最大短板**）
- 错题本 E46/E47 入册
- ⚠️ **安全拦截**：ex09 发票 OCR 的运行日志含真实公司名/税号/开票人姓名/发票号，**commit 前拦下并脱敏**（AiStudy 已是公开仓库）。金额保留以支撑结论。教训：用真实材料做练习时，日志贴进注释前必须先脱敏
- ⚠️ **教练侧问题记录**：Claude 建议加 `temperature=0` 时没记起该后端会静默吞掉该参数（E36 早已记录），由学员指出
- [x] **10/11 Ollama 本地模型**（08-07）：**按实际进度裁剪后只做两件没做过的事**——任务 1、2（连本地/流式/usage/模型外置）已在 ex03/05/07/09 中完成，骨架里改为登记证据不重做
- **KV cache 定量化**（本课主产出）：七档 num_ctx 实测（4K→128K），得出**纯 GPU 区间内呈线性，系数 64 MB/1K**；实际可用显存 19.78 GB（从三档溢出比例反推）+ 空载桌面占用 2.2 GB（nvidia-smi 实测）≈ 22 GB 标称——**两个独立来源闭合**；生产建议 num_ctx=32768（65536 已到临界，需给其他程序留余量）
- **预测-验证闭环**：先用系数预测 81920 会溢出，再实测确认（22 GB、11% 掉 CPU）。学员自行指出「81920 是预测后才产生的数据，不能回头参与推导」——**train/test 泄漏的自觉，教练侧提议有误**
- 27b vs 8b：生成 21.36 vs 62.38 tok/s（2.9x），prefill 144.95 vs 1040.67 tok/s（**7.2x，差距更大**，对 RAG/Agent 的长 prompt 场景意义更强）；结论：调通用 8b，终局与难题用 27b
- 错题本 E48/E49 入册
- ⚠️ 环境坑复发：`OLLAMA_HOST=0.0.0.0` 导致 CLI 连不上——**上次只在单个 shell 里临时修复，用户级变量未改，所以复发**。同时发现 `0.0.0.0` + `OLLAMA_ORIGINS=*` 等于对局域网开放推理服务
- 下一课：**11/11 FastAPI**，之后进入项目① CLI 智能助手

### 阶段0测试进行中
- [x] **A 卷笔试：35.5 / 40**（2026-07-29 提交批改）
  - 满分题：可变默认参数、is/==、pyproject+uv.lock、类型注解、装饰器顺序
  - 扣分集中在 **async 的口头表述**（Q3 GIL 3/5、Q8 阻塞 3/5）——实验数据全对但讲不清"在 await 点让出"与"冻结整个循环"，已入册 [[错题与复盘#E29]]
  - Q6 扣 0.5：只说 dataclass 不做什么，没说它的适用场景（内部可信数据、零依赖）
  - **总评：实验能力 > 表述能力**；后续各阶段加强口头表述抽查（面试里说不清=不会）
- [x] **B 卷实操：56.5 / 60**（2026-07-29 下午完成）
  - 满分项：pydantic 模型（model_validator(mode="after")+Self 正解）、并发与容错（**设计优于标准答案**：try/except 放单站函数内，gather 只收干净结果）、@timed 装饰 async 函数
  - 扣 1.5：表格 url 列宽 100 撑爆终端、表头 is_ok 与字段 ok 不一致、Path 未用 `__file__` 锚点（fetch_and_save 学过没沿用）
  - 扣 2：`def timed(fn):` 无注解 → **E25 免检区第二次复犯**（新卡 E31）
  - 实测彩蛋：`print(e)` 输出空行（httpx 连接异常 str 为空）→ 新卡 E30
- 🎓 **阶段 0 测试总分 92/100，通过**（A 35.5 + B 56.5，门槛 80）
- 第一篇博客草稿已写（`AiStudy/blog/01-*.md`，8 处待学员补充）；两仓库开源前终审通过

### 📊 第 0 周周复盘（2026-07-24 ~ 07-26，首次周复盘）
- **进度**：3 天完成 uv 篇 + 基础01-06（基础篇 6/9），18 个练习全部进仓库，**领先原计划约一周**（原计划第 1 周才开始，基础篇预计 1-1.5 周——按当前速度周二可收官）
- **节奏验证**：白天远程预习 + 晚上动手 ≈ 单篇 30-40 分钟，可持续
- **错题主题分布**：区间边界（off-by-one）、原地操作返回 None、命名规范（复数/遮蔽）、规格意识（return vs print、API 契约顺序）、流程纪律（改完必重跑、注释与代码同步）——后三类是 Java 老兵转语言的典型伤，需持续盯
- **下周（第 1 周，正式周）目标**：基础07-09 收官基础篇 → 进阶篇开跑（类型注解/pydantic/装饰器/async）→ 周末冲击 [[阶段0测试]]（里程碑原定 8-09，有望提前）
- **风险提醒**：明天起回到工作日 2 小时节奏，别拿周末速度要求周中
- ~~待确认事项~~ **已确认（2026-07-24）**：①阶段 3 素材以 LangGraph 1.x / MCP 新规范（2026-07-28 发布）/ Spring AI 2.0+Boot 4+Java 21 为准；②项目③升格为"企业级 Agent 落地项目"。已写入 PLAN.md
- 调研结论存档：RAG+Agent+微调 与 2026 岗位需求吻合；Agent 岗薪资高约 25%；Java/Python 双语是明确加分项

### 第 1 周（2026-07-27 ~ 2026-08-02）
- 学了什么：
- 卡在哪里：
- 下周重点：

## 4 周复盘

### 复盘 1（第 4 周末）
- 进度 vs 计划：
- 需要调整：
