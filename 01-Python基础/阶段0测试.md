---
tags: [AI学习, 阶段0, Python, 测试]
created: 2026-07-24
---

# 阶段 0 测试：Python 基础

> 完成 [[01-Python基础-MOC]] 全部笔记和动手任务后进行。闭卷做笔试部分（不查资料、不用 AI），实操部分可查官方文档但不能用 AI 生成代码。
> **批改方式**：做完后把答案和代码发给 Claude，说「批改阶段0测试」。总分 100，**≥80 分进入阶段 1**；不达标时 Claude 会指出薄弱笔记，复习后可重考（换题）。

## A 卷·笔试（40 分，每题 5 分，预计 30 分钟）

1. 下面代码有一个经典 bug，指出并修复：
   ```python
   def add_item(item, items=[]):
       items.append(item)
       return items
   ```
2. `is` 和 `==` 的区别是什么？给出一个两者结果不同的例子。
3. 用一句话向 Java 同事解释：GIL 存在的情况下，为什么 Python 的 async 还能提升吞吐？
4. `pyproject.toml` 里的依赖和 `uv.lock` 的关系，对应 Maven 里的什么概念？
5. 写出类型注解：一个函数接收「字符串到整数列表的映射」，返回「可能为 None 的字符串」。
6. pydantic 的 `BaseModel` 和 `dataclass` 各适合什么场景？LLM 应用里为什么 pydantic 更常用？
7. 解释这个装饰器的执行顺序：`@a` `@b` `def f(): ...` 等价于什么表达式？
8. 下面代码为什么会卡死事件循环？怎么改？
   ```python
   async def fetch_all(urls):
       for u in urls:
           r = requests.get(u)  # requests 是同步库
   ```

## B 卷·实操（60 分，预计 60-90 分钟）

**题目：并发网站体检工具**。在 `E:\workspace\AiStudy\phase0-python\exam_site_checker.py` 实现：

1. （15 分）定义 pydantic 模型 `SiteReport`：字段 `url: str`、`status: int`、`elapsed_ms: float`、`ok: bool`（status 为 2xx/3xx 时 True）
2. （20 分）用 `httpx.AsyncClient` + `asyncio.gather` 并发检测至少 5 个网站，每个返回一个 `SiteReport`；单站超时 5 秒，超时/异常不许让程序崩溃（status 记 0）
3. （10 分）用装饰器 `@timed` 打印整个检测流程总耗时
4. （10 分）结果按 `elapsed_ms` 升序输出为对齐的表格，并将 JSON 写入 `report.json`（用 `model_dump`）
5. （5 分）全文件类型注解完整，`uv run exam_site_checker.py` 直接可跑

**评分点**：功能正确 + 代码 Pythonic（不写 Java 味 Python：用推导式/f-string/pathlib）+ 异常处理得当。

## 相关笔记

- [[01-Python基础-MOC]] · [[async异步编程]] · [[dataclass与pydantic]] · [[装饰器与上下文管理器]] · [[类型注解与typing]]
- 下一阶段入口：[[02-LLM应用开发-MOC]]
